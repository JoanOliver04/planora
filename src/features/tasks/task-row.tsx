"use client";import {Check} from "lucide-react";import {useState} from "react";import {toast} from "sonner";export function TaskRow({title,emoji,meta,colour="#738f5f",initial=false}:{title:string;emoji:string;meta:string;colour?:string;initial?:boolean}){const[done,setDone]=useState(initial);return <article className="task surface" style={{"--accent":colour} as React.CSSProperties}><button className="task-check" data-done={done} aria-label={done?`Undo ${title}`:`Complete ${title}`} onClick={()=>{setDone(!done);toast.success(!done?"✓":"↶")}}>{done&&<Check size={20}/>}</button><div><div className="task-title" data-done={done}>{emoji} {title}</div><div className="muted" style={{fontSize:12,marginTop:3}}>{meta}</div></div><span className="category-dot"/></article>}
