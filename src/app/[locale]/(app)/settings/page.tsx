"use client";import {useTheme} from "next-themes";import {useLocale,useTranslations} from "next-intl";import {useRouter} from "@/i18n/routing";import {Moon,Sun,Monitor,Languages,Globe2} from "lucide-react";export default function Settings(){const t=useTranslations("Page"),{theme,setTheme}=useTheme(),locale=useLocale(),router=useRouter();return <><header className="topbar"><div><div className="eyebrow">Planora</div><h1 className="title">{t("settings")}</h1></div></header><section className="surface"><div className="settings-row"><span><Languages size={18} style={{display:"inline",marginRight:9}}/>{t("language")}</span><select className="pill" value={locale} onChange={e=>router.replace("/settings",{locale:e.target.value as "es"|"en"})}><option value="es">Español</option><option value="en">English</option></select></div><div className="settings-row"><span><Sun size={18} style={{display:"inline",marginRight:9}}/>{t("theme")}</span><div className="page-actions">{[["light",Sun],["dark",Moon],["system",Monitor]].map(([v,I])=><button className="pill" key={String(v)} aria-pressed={theme===v} onClick={()=>setTheme(String(v))}><I size={16}/></button>)}</div></div><div className="settings-row"><span><Globe2 size={18} style={{display:"inline",marginRight:9}}/>{t("timezone")}</span><span className="muted">Europe/Madrid</span></div></section></>}
