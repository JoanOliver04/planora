import {getTranslations} from "next-intl/server";import {Logo} from "@/components/navigation";import {GoogleButton} from "@/features/auth/google-button";import {Languages,ShieldCheck,Sparkles} from "lucide-react";import Link from "next/link";export default async function Login({params}:{params:Promise<{locale:string}>}){const {locale}=await params,t=await getTranslations("Auth");return <main className="login"><div className="login-card surface"><section className="login-copy"><Logo/><div style={{marginTop:"auto",paddingTop:"8rem"}}><Sparkles size={30}/><h1 className="title" style={{fontSize:"clamp(2.6rem,6vw,4.5rem)",maxWidth:520}}>{t("title")}</h1><p style={{fontSize:"1.15rem",opacity:.78,maxWidth:430}}>{t("subtitle")}</p></div></section><section className="login-form"><h2 style={{fontSize:"1.5rem"}}>Planora</h2><p className="muted" style={{marginBottom:"2rem"}}>{t("subtitle")}</p><GoogleButton/><p className="muted" style={{fontSize:12,lineHeight:1.6,display:"flex",gap:8,marginTop:18}}><ShieldCheck size={28}/>{t("privacy")}</p><div style={{display:"flex",gap:12,marginTop:30}}><Languages size={18}/><Link href={locale==="es"?"/en/login":"/es/login"}>{locale==="es"?"English":"Español"}</Link></div></section></div></main>}
