import {Plus,Search} from "lucide-react";export function ManagementPage({title,action,children}:{title:string;action?:string;children?:React.ReactNode}){return <><header className="topbar"><div><div className="eyebrow">Planora</div><h1 className="title">{title}</h1></div>{action&&<button className="primary"><Plus size={18}/>{action}</button>}</header><div className="pill" style={{width:"100%",marginBottom:"1.2rem"}}><Search size={18}/><input aria-label="Search" placeholder="Search…" style={{border:0,outline:0,background:"transparent",color:"inherit",width:"100%"}}/></div>{children??<div className="empty surface">🌱</div>}</>}
